# Keep LF in the repo AND on checkout everywhere, so shell scripts never get
# CRLF (a \r breaks `set -euo pipefail` and shebangs under bash on Linux).
* text=auto eol=lf

# Binaries: never touch.
*.gz   binary
*.bin  binary
*.png  binary
